<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Xiang Yiheng | Core Repository & Profile</title>
    <style>
        /* 全局样式重置与主题色 */
        :root {
            --bg-color: #0b0f19;
            --card-bg: #151d30;
            --text-main: #f8fafc;
            --text-muted: #94a3b8;
            --accent-blue: #38bdf8;
            --accent-green: #34d399;
            --accent-amber: #fbbf24;
            --border-color: #1e293b;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: 'SF Pro Display', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 60px 20px;
        }

        /* 顶部 Header */
        header {
            text-align: center;
            margin-bottom: 50px;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 40px;
        }

        header h1 {
            font-size: 2.5rem;
            margin: 0 0 10px 0;
            letter-spacing: -0.05em;
            background: linear-gradient(to right, #ffffff, var(--accent-blue));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        header .subtitle {
            color: var(--text-muted);
            font-size: 1rem;
            font-family: 'Courier New', Courier, monospace;
        }

        /* 模块卡片通用样式 */
        .section-card {
            background-color: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            padding: 30px;
            margin-bottom: 24px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.2);
            transition: transform 0.2s ease, border-color 0.2s ease;
        }

        .section-card:hover {
            transform: translateY(-2px);
            border-color: #2e3e5c;
        }

        .section-title {
            font-size: 1.2rem;
            margin-top: 0;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .title-blue { color: var(--accent-blue); }
        .title-green { color: var(--accent-green); }
        .title-amber { color: var(--accent-amber); }

        .section-title::before {
            content: "[ ■ ]";
            margin-right: 10px;
            font-family: monospace;
            font-size: 0.9rem;
        }

        p {
            color: var(--text-muted);
            margin: 0 0 15px 0;
            font-size: 1.05rem;
        }

        /* 链接样式 */
        .inline-link {
            color: var(--accent-blue);
            text-decoration: none;
            border-bottom: 1px dashed var(--accent-blue);
            transition: color 0.2s, border-bottom 0.2s;
        }

        .inline-link:hover {
            color: #7dd3fc;
            border-bottom: 1px solid #7dd3fc;
        }

        /* 列表样式 */
        ul {
            margin: 0;
            padding-left: 20px;
            color: var(--text-muted);
        }

        li {
            margin-bottom: 8px;
        }

        li strong {
            color: var(--text-main);
        }

        /* 动态按钮与隐藏区域 */
        .btn-container {
            text-align: center;
            margin: 40px 0;
        }

        .toggle-btn {
            background-color: transparent;
            color: var(--accent-blue);
            border: 1px solid var(--accent-blue);
            padding: 12px 24px;
            font-size: 1rem;
            border-radius: 6px;
            cursor: pointer;
            font-family: 'Courier New', Courier, monospace;
            font-weight: bold;
            transition: all 0.3s ease;
            box-shadow: 0 0 15px rgba(56, 189, 248, 0.1);
        }

        .toggle-btn:hover {
            background-color: var(--accent-blue);
            color: var(--bg-color);
            box-shadow: 0 0 25px rgba(56, 189, 248, 0.3);
        }

        #home-profile {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.5s ease-out, opacity 0.5s ease-out;
            opacity: 0;
        }

        #home-profile.expanded {
            max-height: 500px; /* 足够容纳内容的高度 */
            opacity: 1;
            margin-bottom: 24px;
        }

        /* 页脚 */
        footer {
            text-align: center;
            margin-top: 60px;
            color: #475569;
            font-size: 0.85rem;
            font-family: monospace;
        }
    </style>
</head>
<body>

    <div class="container">
        
        <header>
            <h1>Xiang Yiheng</h1>
            <div class="subtitle">Personal Infrastructure // Academic Repository</div>
        </header>

        <div class="btn-container">
            <button class="toggle-btn" id="profile-toggle-btn">> INITIALIZE HOME PROFILE</button>
        </div>

        <div id="home-profile" class="section-card" style="border-color: var(--accent-blue);">
            <h2 class="section-title title-blue">Roots & Foundation</h2>
            <p><strong>Origin:</strong> Deeply rooted in Ningbo, Zhejiang — a city renowned for its profound cultural heritage and thriving spirit of exploration.</p>
            <p>Home serves as the foundational anchor, providing the essential support structure and balance required to pursue global academic trajectories and complex intellectual challenges.</p>
        </div>

        <div class="section-card">
            <h2 class="section-title title-blue">Academic Institution</h2>
            <p>Currently pursuing an advanced international curriculum at <strong>Ulink College of Shanghai</strong>, an elite institution dedicated to fostering global perspectives and academic excellence.</p>
            <p>Ulink College provides a highly rigorous environment that empowers students to challenge boundaries and prepare thoroughly for world-class higher education.</p>
            <p style="margin-bottom: 0;">
                <span style="color: var(--text-main);">Official Portal:</span> 
                <a href="https://ulink.cn" target="_blank" class="inline-link">ulink.cn</a>
            </p>
        </div>

        <div class="section-card">
            <h2 class="section-title title-green">Experience & Future Outlook</h2>
            <p>Began the academic journey at <strong>Ningbo Xingning Middle School</strong>, where a solid foundational knowledge base and analytical mindset were established.</p>
            <p>Bridging this robust past experience with ambitious future trajectories, the core objective is to transition successfully into higher tiers of global academia, translating foundational skills into high-impact research capabilities.</p>
        </div>

        <div class="section-card">
            <h2 class="section-title title-amber">Key Achievements</h2>
            <ul>
                <li><strong>NEC (National Economics Challenge):</strong> Demonstrated deep analytical competence and strategic problem-solving capacities in high-level economic frameworks.</li>
                <li><strong>World Robot Contest:</strong> Secured the prestigious <strong>First Prize (1st Prize)</strong>, reflecting advanced technical proficiency, systemic design engineering, and collaborative team execution under intense competitive pressure.</li>
            </ul>
        </div>

        <div class="section-card">
            <h2 class="section-title title-blue">Future Aspirations</h2>
            <p>Currently configuring all academic vectors toward a singular objective: gaining admission into the <strong>UK Top 5 Universities</strong>.</p>
            <p>The aim is to immerse into world-class research infrastructure and engage in deep specialization within advanced academic disciplines.</p>
        </div>

        <div class="section-card">
            <h2 class="section-title title-green">Recent Updates</h2>
            <p style="margin-bottom: 0;">Successfully concluded the demanding <strong>CIE (Cambridge International Examinations)</strong>. It proved to be a remarkably intense and hard experience, testing both mental endurance and comprehensive academic mastery. The challenge has successfully consolidated core domain competencies.</p>
        </div>

        <footer>
            © 2026 Xiang Yiheng. All intellectual property reserved. // Powered by Coffee & Curiosity.
        </footer>

    </div>

    <script>
        const toggleBtn = document.getElementById('profile-toggle-btn');
        const homeProfile = document.getElementById('home-profile');

        toggleBtn.addEventListener('click', () => {
            homeProfile.classList.toggle('expanded');
            if (homeProfile.classList.contains('expanded')) {
                toggleBtn.textContent = "[ TERMINATE HOME PROFILE VIEW ]";
                // 丝滑滚动到展开的内容
                setTimeout(() => {
                    homeProfile.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                }, 200);
            } else {
                toggleBtn.textContent = "> INITIALIZE HOME PROFILE";
            }
        });
    </script>
</body>
</html>
